{% block title %}Section: Products{% endblock %}
{% block lead %}{% endblock %}
{% block content %}
{% include "doc-head" %}
<div id="page" class="sbx shop layout-1col">

	<div class="span8 col-main">
		<div class="page-header">
			<h1>Product Cart Listing w/ Button <span>(Default Table Layout)</span></h1>
			<p class="lead">Addition of the 'view details' button which will open up a modal.</p>
		</div>
		{% include "section-products" %}

		<div class="page-header">
			<h1>Product Cart Listing w/ Dividers <span>(Default Table Layout)</span></h1>
			<p class="lead">Divider is just a ROW that spans the entire table w/ custom html.</p>
		</div>
		{% include "section-products-rowheadings" %}

		<div class="page-header">
			<h1>Product Cart Listing w/ Tabs <span>(Default Table Layout)</span></h1>
			<p class="lead">Splits the products into idividual tabs. In this scenario there is a table of products per tab. So if a product is assigned to the ALL tab, as well as the FEATURED tab, it would be rendered twice.</p>
		</div>
		{% include "section-products-tabs" %}

		<div class="page-header">
			<h1>Product Cart Listing w/ FAKE Tabs <span>(Default Table Layout)</span></h1>
			<p class="lead">In this scenario the products are listed in 1 table, and the tabs just hide, via jQuery/CSS the items that aren't assigned to the tab. This way there is only 1 instance of each product, no matter what tab you click. This is the method me, Tim and Rene discussed using to cut down on JS complexity.</p>
		</div>
		{% include "section-products-tabs-fake" %}
	</div>
	<div class="span4 col-sidebar">

	</div>

</div>

{% include "modal-productdetail" %}

{% include "doc-foot" %}
{% endblock %}

{% block footerjs %}
  <script type="text/javascript">

  $(document).ready(function(){
	    $('#shopCartTabNav-FAKE').tab();
	    

	    $('#shopCartTabNav-FAKE a[href="#all-products-fake"]').click(function (e) {
	        e.preventDefault();
	        //$(this).tab('show');

	        $( ".tab-pane .cart-item" ).removeClass( "is-hidden" );
	        $( ".tab-pane .cart-item-featured" ).removeClass( "is-hidden" );
	        $( ".tab-pane .cart-item-onsale" ).removeClass( "is-hidden" );
	    })

	    $('#shopCartTabNav-FAKE a[href="#featured-products-fake"]').click(function (e) {
	        e.preventDefault();
	        //$(this).tab('show');
	        
	        $( ".tab-pane .cart-item" ).removeClass( "is-hidden" );
	        $( ".tab-pane .cart-item-featured" ).removeClass( "is-hidden" );
	        $( ".tab-pane .cart-item-onsale" ).removeClass( "is-hidden" );

	        $( ".tab-pane .cart-item" ).addClass( "is-hidden" );
	        $( ".tab-pane .cart-item-onsale" ).addClass( "is-hidden" );
	        
	        
	    })

	    $('#shopCartTabNav-FAKE a[href="#sale-products-fake"]').click(function (e) {
	        e.preventDefault();
	        //$(this).tab('show');
			$( ".tab-pane .cart-item" ).removeClass( "is-hidden" );
	        $( ".tab-pane .cart-item-featured" ).removeClass( "is-hidden" );
	        $( ".tab-pane .cart-item-onsale" ).removeClass( "is-hidden" );

	        $( ".tab-pane .cart-item" ).addClass( "is-hidden" );
	        $( ".tab-pane .cart-item-featured" ).addClass( "is-hidden" );
	    })
    });

</script> 
{% endblock %}